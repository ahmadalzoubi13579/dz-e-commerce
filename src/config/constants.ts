import { APP_ENV_MODE } from '../app/shared/types';

/** Tax rate for products */
export const TAX_RATE = 0.19;

// can be moved to NEXT_PUBLIC_BASE_API_URL env variable
export const BASE_API_URL = 'http://localhost:3000/api/';

// can be moved to NEXT_PUBLIC_BASE_URL env variable
export const BASE_URL = 'http://localhost:3000/';

// can be moved to NEXT_PUBLIC_APP_ENV env variable
export const APP_ENV: APP_ENV_MODE = 'dev';

export const APP_VERSION = '1.0.0';
